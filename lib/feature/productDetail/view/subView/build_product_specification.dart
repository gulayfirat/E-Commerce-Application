part of '../product_detail_view.dart';

class BuildProductSpecification extends StatelessWidget {
  const BuildProductSpecification({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        "Specification TabView No design",
      ),
    );
  }
}
